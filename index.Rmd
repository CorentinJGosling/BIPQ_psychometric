---
title: 'Supplementary for : '
author: "Miss Blandine, Prof Yannick, and the rest (the plebe people)"
date: '2025-09-17'
output:
  html_document:
    toc: true
    toc_float: true
    encoding: UTF-8
  pdat_document:
    toc: true
---


```{r, echo=FALSE}
# control + alt + i = crée un bloc de code
# les #, ##, ### hors des blocs de code = niveaux de titre (1, 2, 3)
# kniter pour rendre le format HTML
# créer un ficher .gitignore (tu vas dans le terminal click droit sur le dossier git et tu tapes : touch .gitignore) et rentrer le nom de votre fichier de données pour ne pas le rendre accessible gratuitement sur github (quoi que si tu fais l'erreur, j'en profiterai!! :) )
```


```{r, echo=FALSE, message=FALSE, results='hide', warning=FALSE}

# j'ai enlevé les install packages, énormément de place pour peu de bénéfice, on sait tous installer un package ici, nous sommes des gens civilisés :) 
# {r, echo=FALSE, message=FALSE, results='hide', warning=FALSE} => veut dire de cacher le text (echo=FALSE) et l'output (des warning au chargement de chaque package)
# globalement c'est une très mauvaise pratique de charger toutes les library car ça peut créer des conflicts. Pour les packages dont on utilise une seule fonction une fois, préférer nom_dupackage::nom_de_la_fonction()

library(summarytools)
library(psych)
library(psychTools)
library(psy)
library(lavaan)
library(parameters)
library(datawizard)
library(dplyr)
library(kableExtra)
library(semTools)
library(tidySEM)
library(lavaanPlot)
library(BifactorIndicesCalculator)
library(tidyLPA)
library(table1)
library(readxl)
library(psych)
library(corrplot)
library(ggplot2)
library(MASS)
library(yhat)
library(boot)
library(lavaanPlot)

library(semPlot)
```



```{r, echo=FALSE, message=FALSE, results='hide', warning=FALSE}

dat <-read_excel("C:/Users/Stagiaire_CER2025/Recherche/CompareDep/BIPQ/Mai2026/Data_BIPQ.Xlsx")

num_vars <- c(
  "age", 
  "stigma_percept_1","stigma_percept_2","stigma_percept_3","stigma_percept_4",
  "stigma_percept_5","stigma_percept_6","stigma_percept_7","stigma_percept_8", "Score_lubben", "score_PHQ9", "DIAG_NOW_3_Age", "nb_maladies_psy", "nb_maladies_somatiques", "nb_PEC1", "nb_PEC2","Score_BIPQ", "Score_whodas", "score_GAD7", "Score_autostigma"
)

to_num <- function(x) {
  x <- trimws(as.character(x))
  x <- gsub(",", ".", x, fixed = TRUE)
  suppressWarnings(as.numeric(x))
}

dat[num_vars] <- lapply(dat[num_vars], to_num)



#### Data management express
# recup année début d'épisode en cours

library(lubridate)
# Convertir d'abord les dates (adapter le format si besoin, voir plus bas)
dat$DIAG_NOW_4 <- as.POSIXct(dat$DIAG_NOW_4, tz = "Europe/Paris")
dat$DateValidation_J0 <- as.POSIXct(dat$DateValidation_J0, tz = "Europe/Paris")

# Puis calculer
dat$annee_dep <- year(dat$DIAG_NOW_4)
dat$diff_mois <- interval(dat$DIAG_NOW_4, dat$DateValidation_J0) %/% months(1)

# Numeric
dat$diff_mois <- as.numeric(dat$diff_mois)
dat$annee_dep <- as.numeric(dat$annee_dep)

dat <- dat %>%
  mutate(age_classe = case_when(
    age >= 18 & age < 25 ~ "18-24",
    age >= 25 & age < 35 ~ "25-34",
    age >= 35 & age < 45 ~ "35-44",
    age >= 45 & age < 55 ~ "45-54",
    age >= 55 & age < 65 ~ "55-64",
    age >= 65 & age < 75 ~ "65-74",
    age >= 75 & age <= 100 ~ "75+",
    TRUE ~ NA_character_
  ))

dat <- dat %>%
  mutate(PEC_12 = case_when(
    PEC_12 == 5 ~ "Je ne préfère pas répondre à cette question",
    TRUE ~ PEC_12
  ))



# Garder uniquement les variables d’intérêt + poids pour l'EFA
items <- c("stigma_percept_1", "stigma_percept_2", "stigma_percept_3", "stigma_percept_4",
           "stigma_percept_5", "stigma_percept_6", "stigma_percept_7", "stigma_percept_8")

data_efa <- dat[, c(items)]


```


# S1. Sample description
```{r table1_socio, results='asis', echo=FALSE}
caption <- "Charactéristiques sociodémographiques"

dat$Sexe<-factor(dat$Sexe) 
dat$age_classe<-factor(dat$age_classe) 
dat$situation_emploi_ini<-factor(dat$situation_emploi_ini) 
dat$situation_financiere<-factor(dat$situation_financiere) 
dat$diplome_ini<-factor(dat$diplome_ini)
dat$DEM_3_Aide<-factor(dat$DEM_3_Aide)

label(dat$age_classe) <- "Classe d'âge"
label(dat$situation_emploi_ini) <- "Situation d'emploi"
label(dat$situation_financiere) <- "Situation financière"
label(dat$diplome_ini) <- "Niveau de diplôme"
label(dat$DEM_3_Aide) <- "Statut d'aidant"
label(dat$Score_lubben) <- "Score Lubben"

# peux etre ici séparer par H/F pour + lisibilité ? On ne peut pas car on a pas assez d'hommes 
table1(~  Sexe + age_classe + age + situation_emploi_ini + situation_financiere + diplome_ini+ DEM_3_Aide + Score_lubben , data=dat, caption=caption)

```                             


```{r table1_clinique, results='asis', echo=FALSE}
caption <- "Charactéristiques cliniques"

dat$score_PHQ9_f<-factor(dat$score_PHQ9_f) 
dat$DIAG_7<-factor(dat$DIAG_7) 
dat$DIAG_NOW_1<-factor(dat$DIAG_NOW_1) 
dat$DIAG_NOW_3<-factor(dat$DIAG_NOW_3) 
dat$DIAG_NOW_2<-factor(dat$DIAG_NOW_2)
dat$DIAG_6<-factor(dat$DIAG_6) 
dat$PEC_PREV_10<-factor(dat$PEC_PREV_10) 
dat$PEC_18<-factor(dat$PEC_18)
dat$PEC_12<-factor(dat$PEC_12) 
dat$PEC_NOW_9<-factor(dat$PEC_NOW_9)

label(dat$score_PHQ9_f) <- "Classe score PHQ9"
label(dat$score_PHQ9) <- "Score PHQ9"
label(dat$DIAG_7) <- "Diagnostic trouble bipolaire"
label(dat$DIAG_NOW_1) <- "Diagnostic posé par : "
label(dat$DIAG_NOW_3) <- "Nombre d'épisodes"
label(dat$DIAG_NOW_3_Age) <- "Age au 1er épisode"
label(dat$diff_mois) <- "Durée de l'épisode en cours (mois)"
label(dat$DIAG_NOW_2) <- "Post partum"
label(dat$nb_maladies_psy) <- "Comorbiditées psy"
label(dat$nb_maladies_somatiques) <- "Comorbidités somatiques"
label(dat$nb_PEC1) <- "Nombre d'antidépresseurs"
label(dat$nb_PEC2) <- "Nombre de Thymorégulateurs"
label(dat$DIAG_6) <- "Nombre d'hospiatalisations psy"
label(dat$PEC_PREV_10) <- "Suivi d'une psychothérapie au cours de la vie"
label(dat$PEC_18) <- "Recours à la médecine alternative"
label(dat$PEC_12) <- "Participation réhabilitation cognitive"
label(dat$PEC_NOW_9) <- "Psychothérapie dans les 12 derniers mois"
label(dat$Score_BIPQ) <- "Score BIPQ"
label(dat$Score_whodas) <- "Score whodas"
label(dat$score_GAD7) <- "Score GAD7"
label(dat$Score_autostigma) <- "Score autostigma"

# passer en EN + | Sexe aussi je pense -> sexe tjrs mm pb et en(j'ai déjà fais une table desc pour le papier je verrai à la fin si je prend le temps de le faire avec ça )
table1(~ score_PHQ9_f + score_PHQ9 + DIAG_7 + DIAG_NOW_1 + DIAG_NOW_3 + DIAG_NOW_3_Age + diff_mois + DIAG_NOW_2 + nb_maladies_psy + nb_maladies_somatiques + nb_PEC1 + nb_PEC2 + DIAG_6 + PEC_PREV_10 + PEC_18 + PEC_12 + PEC_NOW_9 + Score_BIPQ + Score_whodas + score_GAD7 + Score_autostigma, data=dat, caption=caption)


```

# S2. Variable description{.tabste}


## BIPQ
```{r, , echo=FALSE}
# .tabset sert à organiser plus proprement
#cherry on the cake des résidus de modèles

# BIPQ
ggplot(dat, aes(x = Score_BIPQ)) +
  geom_histogram(aes(y = after_stat(density)),
                 bins = 10, fill = "grey80", color = "black",
                 na.rm = TRUE) +
  geom_density(na.rm = TRUE) +
  labs(
    title = "BIPQ score distribution",
    x = "BIPQ score",
    y = "Density"
  ) +
  theme_minimal()
```


## PHQ-9
```{r, , echo=FALSE}
# PHQ-9
ggplot(dat, aes(x = score_PHQ9)) +
  geom_histogram(aes(y = after_stat(density)),
                 bins = 10, fill = "grey80", color = "black",
                 na.rm = TRUE) +
  geom_density(na.rm = TRUE) +
  labs(
    title = "PHQ-9 score distribution",
    x = "PHQ-9 score",
    y = "Density"
  ) +
  theme_minimal()
```

## GAD-7
```{r, , echo=FALSE}
# GAD-7
ggplot(dat, aes(x = score_GAD7)) +
  geom_histogram(aes(y = after_stat(density)),
                 bins = 10, fill = "grey80", color = "black",
                 na.rm = TRUE) +
  geom_density(na.rm = TRUE) +
  labs(
    title = "GAD-7 score distribution",
    x = "GAD-7 score",
    y = "Density"
  ) +
  theme_minimal()
```

## SSS-S
```{r, , echo=FALSE}
# SSS
ggplot(dat, aes(x = Score_autostigma)) +
  geom_histogram(aes(y = after_stat(density)),
                 bins = 10, fill = "grey80", color = "black",
                 na.rm = TRUE) +
  geom_density(na.rm = TRUE) +
  labs(
    title = "SSS-S score distribution",
    x = "SSS-S score",
    y = "Density"
  ) +
  theme_minimal()



```

## WHODAS-12
```{r, , echo=FALSE}
# SSS
ggplot(dat, aes(x = Score_whodas)) +
  geom_histogram(aes(y = after_stat(density)),
                 bins = 10, fill = "grey80", color = "black",
                 na.rm = TRUE) +
  geom_density(na.rm = TRUE) +
  labs(
    title = "WHODAS-12 score distribution",
    x = "WHODAS-12 score",
    y = "Density"
  ) +
  theme_minimal()



```


## Normality of the distribution of errors in the BIPQ items

```{r,echo=FALSE}
## ici, regarde le package kableExtra pour faire des tables + jolies
## https://cran.r-project.org/web/packages/kableExtra/vignettes/awesome_table_in_html.html
# tous mes items ont été remis dans le bon sens préalablement
items <- c(
  "stigma_percept_1", "stigma_percept_2", "stigma_percept_3", "stigma_percept_4",
  "stigma_percept_5", "stigma_percept_6", "stigma_percept_7", "stigma_percept_8"
)

data_items <- dat[, items]

results_residuals <- list()

for (i in 1:(length(items) - 1)) {
  for (j in (i + 1):length(items)) {

    x <- data_items[[items[i]]]
    y <- data_items[[items[j]]]

    # Nettoyage NA
    ok <- is.finite(x) & is.finite(y)
    x <- x[ok]
    y <- y[ok]

    if (length(x) < 10) next

    # Modèle linéaire
    fit <- lm(y ~ x)
    res <- resid(fit)

    # Test de normalité
    sh <- shapiro.test(res)

    # Stockage
    results_residuals[[paste(items[j], "~", items[i])]] <- list(
      predictor = items[i],
      outcome   = items[j],
      n         = length(res),
      W         = unname(sh$statistic),
      p_value   = sh$p.value,
      residuals = res
    )
  }
}

residuals_summary <- do.call(rbind, lapply(names(results_residuals), function(name) {
  x <- results_residuals[[name]]
  data.frame(
    model = name,
    predictor = x$predictor,
    outcome = x$outcome,
    n = x$n,
    W = x$W,
    p_value = x$p_value,
    stringsAsFactors = FALSE
  )
}))

dt<-residuals_summary[, c("model", "W", "p_value")]
dt %>%
  kbl() %>%
  kable_styling()

```


## Distribution of association residues

```{r, echo=FALSE, message=FALSE, warning=FALSE, fig.width=8.5, fig.height=4}
plot_resid <- function(yvar, label, xvar="Score_BIPQ", min_n=10) {

  if (!all(c(xvar, yvar) %in% names(dat))) {
    cat(sprintf("\n%s : variable manquante\n", label))
    return(invisible(NULL))
  }

  x <- dat[[xvar]]
  y <- dat[[yvar]]

  ok <- is.finite(x) & is.finite(y)
  n  <- sum(ok)

  cat(sprintf("\n%s : n=%d\n", label, n))

  if (n < min_n) {
    cat(sprintf("%s : pas assez de données (min_n=%d)\n", label, min_n))
    return(invisible(NULL))
  }

  x2 <- x[ok]; y2 <- y[ok]

  sx <- sd(x2, na.rm=TRUE)
  sy <- sd(y2, na.rm=TRUE)

  cat(sprintf("%s : sd(x)=%.6f | sd(y)=%.6f\n", label, sx, sy))

  if (!is.finite(sx) || !is.finite(sy) || sx == 0 || sy == 0) {
    cat(sprintf("%s : impossible de fitter (variance nulle ou invalide) -> skip\n", label))
    return(invisible(NULL))
  }

  # Standardisation manuelle (au lieu de scale() dans lm)
  zx <- (x2 - mean(x2)) / sx
  zy <- (y2 - mean(y2)) / sy

  fit <- lm(zy ~ zx)
  res <- resid(fit)

  oldpar <- par(no.readonly = TRUE)
  on.exit(par(oldpar), add = TRUE)
  par(mfrow = c(1,2))
  hist(res, breaks = 20, main = paste0("Residuals ", label, " (n=", n, ")"), xlab = "Residuals")
  qqnorm(res, main = paste0("QQ-plot ", label))
  qqline(res)
}

plot_resid("score_GAD7",       "GAD-7 ~ BIPQ")
plot_resid("score_PHQ9",       "PHQ-9 ~ BIPQ")
plot_resid("Score_whodas",     "WHODAS-12 ~ BIPQ")
plot_resid("Score_autostigma", "SSS-S ~ BIPQ")


```


# S3. Internal consistency{.tabset}

## Distribution des items

```{r,message=FALSE, warning=FALSE }

vars <- paste0("stigma_percept_", 1:8)

d <- descr(
  data_efa[vars],
  stats = c("skewness", "kurtosis"),
  transpose = TRUE
)

# Converts the summarytools object into a usable data.frame
tab <- as.data.frame(d)

# The columns are generally called “Skewness” / “Kurtosis” (spelling varies depending on version).

skew_kurt_table <- data.frame(
  variable = rownames(tab),
  skewness = as.numeric(tab[[grep("skew", names(tab), ignore.case = TRUE, value = TRUE)[1]]]),
  kurtosis = as.numeric(tab[[grep("kurt", names(tab), ignore.case = TRUE, value = TRUE)[1]]]),
  row.names = NULL
)

skew_kurt_table$skewness <- round(skew_kurt_table$skewness, 2)
skew_kurt_table$kurtosis <- round(skew_kurt_table$kurtosis, 2)


# -----------------------------
# 2)  Global data visualization with dfSummary (mini histograms + NA, etc.)
# -----------------------------
dfs <- dfSummary(
  data_efa[vars],
  style      = "grid",
  graph.col  = TRUE,   # mini-charts in the Graph column
  valid.col  = TRUE,
  na.col     = TRUE
)


# or console/HTML rendering
print(dfs, method = "render")


# display skewness and kurtosis
skew_kurt_table%>%
  kbl() %>%
  kable_styling()%>%
  column_spec(1, bold = TRUE)





```


## ICC
```{r}

data_efa$'item 1'<- data_efa$stigma_percept_1
data_efa$'item 2'<- data_efa$stigma_percept_2
data_efa$'item 3'<- data_efa$stigma_percept_3
data_efa$'item 4'<- data_efa$stigma_percept_4
data_efa$'item 5'<- data_efa$stigma_percept_5
data_efa$'item 6'<- data_efa$stigma_percept_6
data_efa$'item 7'<- data_efa$stigma_percept_7
data_efa$'item 8'<- data_efa$stigma_percept_8

items <- data_efa[, c('item 1',
                      'item 2',
                      'item 3',
                      'item 4',
                      'item 5',
                      'item 6',
                      'item 7',
                      'item 8')]

# Calculate Spearman's correlation matrix
iic <- cor(items, method = "spearman", use = "pairwise.complete.obs")
round(iic, 2)  %>%
  kbl() %>%
  kable_styling()%>%
  column_spec(1, bold = TRUE)



# simple Heatmap 
corrplot(iic, method = "color", type = "upper",
         addCoef.col = "black", # ajoute les valeurs
         tl.col = "black", tl.srt = 45,
         diag = FALSE)

```

## ITC
```{r, message=FALSE,  warning=FALSE}
items <- data_efa[vars]

# Cronbach alpha
alpha_result <- psych::alpha(items, check.keys = FALSE)


# Corrected item-total correlations (r.drop)
r_drop <- alpha_result$item.stats$r.drop
names(r_drop) <- colnames(items)
r_drop

alpha_table <- data.frame(
  item = rownames(alpha_result$item.stats),
  mean = alpha_result$item.stats$mean,
  sd   = alpha_result$item.stats$sd,
  r_drop = alpha_result$item.stats$r.drop,
  alpha_if_deleted = alpha_result$alpha.drop$raw_alpha,
  row.names = NULL
)

alpha_table%>%
  kbl() %>%
  kable_styling()%>%
  column_spec(1, bold = TRUE)

```

Explanations: alpha_result$total$raw_alpha: overall alpha

r.drop: corrected item–total correlation (often we like to see > 0.30)

alpha_if_deleted: if deleting the item significantly increases alpha → item possibly problematic

## Cronbach 

```{r, message=FALSE, warning=FALSE}
items <- data_efa[vars]

# Cronbach alpha
alpha_result <- psych::alpha(items, check.keys = FALSE)

# Alpha values
alpha_result$total$raw_alpha

```

## Omega

```{r, message=FALSE,  warning=FALSE}
# total Omega  (unidimensional)
omega_result <- omega(items, nfactors = 1, fm = "ml")  # fm = "ml"  maximum likelihood

# print results
omega_result

```

## Floor ceiling

```{r, message=FALSE,  warning=FALSE}

# items = data.frame of numeric items
floor_ceiling <- t(sapply(items, function(x) {
  x <- x[is.finite(x)]
  n <- length(x)
  if (n == 0) return(c(Floor = NA_real_, Ceiling = NA_real_))

  floor_effect   <- sum(x == min(x)) / n * 100
  ceiling_effect <- sum(x == max(x)) / n * 100

  c(Floor = round(floor_effect, 1), Ceiling = round(ceiling_effect, 1))
}))

floor_ceiling <- as.data.frame(floor_ceiling)
floor_ceiling$item <- rownames(floor_ceiling)
rownames(floor_ceiling) <- NULL



stats <- data.frame(
  item = names(items),
  n    = sapply(items, function(x) sum(is.finite(x))),
  min  = sapply(items, function(x) min(x, na.rm = TRUE)),
  max  = sapply(items, function(x) max(x, na.rm = TRUE)),
  mean = sapply(items, function(x) mean(x, na.rm = TRUE)),
  sd   = sapply(items, function(x) sd(x, na.rm = TRUE))
)

report_tbl <- stats %>%
  left_join(floor_ceiling, by = "item") %>%
  mutate(
    mean = round(mean, 2),
    sd   = round(sd, 2)
  )

report_tbl %>%
  kbl(caption = "Descriptifs + Floor/Ceiling effects by item") %>%
  kable_styling(full_width = FALSE) %>%
  column_spec(1, bold = TRUE)


```

# S4. Structural validity{.tabset}

## unidimensional CFA

```{r}

data_efa <- data_efa %>%
  rename(
    item1 = `item 1`,
    item2 = `item 2`,
    item3 = `item 3`,
    item4 = `item 4`,
    item5 = `item 5`,
    item6 = `item 6`,
    item7 = `item 7`,
    item8 = `item 8`
  )

cfa_model <- '
BIPQ =~ item1 + item2 + item3 + item4 +
        item5 + item6 + item7 + item8
'

fit <- cfa(cfa_model, data = data_efa, estimator = "MLR") # MLR = robust to non-normality
summary(fit, fit.measures = TRUE, standardized = TRUE, rsquare = TRUE)

fitMeasures(fit, c("cfi", "tli", "rmsea", "srmr"))

semPaths(fit, what = "path", whatLabels = "std", layout = "tree",
         color = "white", edge.color = "black", fade = FALSE,
         nCharNodes = 0, edge.label.cex = 0.9,
         sizeMan = 8, sizeLat = 10, mar = c(5, 5, 5, 5))
```


## CFA comparison

For model comparison, CFA does not work, probably due to a problem related to covariance.

```{r, warning=FALSE}

### model 1 

cfa_model_multi <- '
F1 =~ stigma_percept_1 + stigma_percept_5 + stigma_percept_6 + stigma_percept_8
F2 =~ stigma_percept_3 + stigma_percept_4 + stigma_percept_7
# Item 2 n’appartient à aucun facteur, il reste indépendant
'
fit_multi <- cfa(cfa_model_multi, data = data_efa, estimator = "MLR")
summary(fit_multi, fit.measures = TRUE, standardized = TRUE, rsquare = TRUE)


### model 2 

cfa_model_multi_2 <- '
F1 =~ stigma_percept_1 + stigma_percept_2 + stigma_percept_5 + stigma_percept_6 + stigma_percept_8
F2 =~ stigma_percept_3 + stigma_percept_4 + stigma_percept_7
'
fit_multi_2 <- cfa(cfa_model_multi_2, data = data_efa, estimator = "MLR")

summary(fit_multi_2, fit.measures = TRUE, standardized = TRUE, rsquare = TRUE)


```

# S5. Hypothesis testing{.tabset} 

## Convergent validity

### Simple correlation
```{r, warning=FALSE}

dat_bis <- dat %>%
  dplyr::select(score_PHQ9, Score_autostigma, Score_BIPQ, Score_whodas, score_GAD7)

cor_spearman2 <- cor(dat_bis$Score_BIPQ, dat_bis$score_PHQ9, method = "spearman", use = "complete.obs")
cor.test(dat_bis$Score_BIPQ, dat_bis$score_PHQ9, method = "spearman")

cor_spearman3 <- cor(dat_bis$Score_BIPQ, dat_bis$score_GAD7, method = "spearman", use = "complete.obs")

cor.test(dat_bis$Score_BIPQ, dat_bis$score_GAD7, method = "spearman")


```

### Latent model 

#### BIPQ ~ PHQ9
#### simple model (free correlation)
```{r, warning=FALSE}

var_phq <- c("id_unik", "score_PHQ9", "interet_plaisir",	"triste_deprime",	"sommeil",	"fatigue",	"appetit",	"opinion_soi",	"concentration",	"activite",	"consequence_pb",
             
             "Score_BIPQ", "stigma_percept_1", "stigma_percept_2", "stigma_percept_3", "stigma_percept_4","stigma_percept_5", "stigma_percept_6", "stigma_percept_7", "stigma_percept_8"
)
data_phq <-dat[, c(var_phq)]


cfa_model_PHQ <- '
BIPQ =~ stigma_percept_1 + stigma_percept_5 + stigma_percept_6 + stigma_percept_8 + stigma_percept_3 + stigma_percept_4 + stigma_percept_7 + stigma_percept_2
PHQ9 =~ interet_plaisir + triste_deprime + sommeil + fatigue + appetit + opinion_soi + concentration + activite + consequence_pb
'
fit_PHQ <- cfa(cfa_model_PHQ, data = data_phq, estimator = "MLR")
#summary(fit_PHQ, fit.measures = TRUE, standardized = TRUE, rsquare = TRUE)
fitMeasures(fit_PHQ, c("cfi", "tli", "rmsea", "srmr"))

```

#### with correlation ==1

```{r, warning=FALSE}
cfa_PHQ_corr <- '
BIPQ =~ stigma_percept_1 + stigma_percept_5 + stigma_percept_6 + 
        stigma_percept_8 + stigma_percept_3 + stigma_percept_4 + 
        stigma_percept_7 + stigma_percept_2

PHQ9 =~ interet_plaisir + triste_deprime + sommeil + fatigue + appetit + opinion_soi + concentration + activite + consequence_pb

# correlation constrained to 1
BIPQ ~~ 1*PHQ9
'

fit_PHQ_corr <- cfa(
  cfa_PHQ_corr,
  data = data_phq,
  estimator = "MLR"
)

#summary(fit_PHQ_corr, fit.measures = TRUE, standardized = TRUE)
fitMeasures(fit_PHQ_corr, c("cfi", "tli", "rmsea", "srmr"))

# test

lavTestLRT(fit_PHQ, fit_PHQ_corr)

```

Explanations: If the test is significant: the constraint (correlation = 1) degrades the model, so the factors are not equivalent. 


#### 1 factor model 
```{r}
cfa_model_PHQ_1 <- '
BIPQPHQ9 =~ stigma_percept_1 + stigma_percept_5 + stigma_percept_6 + stigma_percept_8 + stigma_percept_3 + stigma_percept_4 + stigma_percept_7 + stigma_percept_2
 + interet_plaisir + triste_deprime + sommeil + fatigue + appetit + opinion_soi + concentration + activite + consequence_pb
'
fit_PHQ_1 <- cfa(cfa_model_PHQ_1, data = data_phq, estimator = "MLR")
#summary(fit_PHQ_1, fit.measures = TRUE, standardized = TRUE, rsquare = TRUE)

fitMeasures(fit_PHQ_1, c("cfi", "tli", "rmsea", "srmr"))

```

#### BIPQ~GAD7
#### Simple model (free correlation)

```{r}
var_gad <- c("id_unik", "score_GAD7", "nerveux",	"inquiet",	"inquiet_exces",	"difficult_detente",	"agitation",	"tendance_contrarie",	"sentiment_peur",
             "Score_BIPQ", "stigma_percept_1", "stigma_percept_2", "stigma_percept_3", "stigma_percept_4","stigma_percept_5", "stigma_percept_6", "stigma_percept_7", "stigma_percept_8"
)
data_gad<-dat[, c(var_gad)]

cfa_model_GAD <- '
BIPQ =~ stigma_percept_1 + stigma_percept_5 + stigma_percept_6 + stigma_percept_8 + stigma_percept_3 + stigma_percept_4 + stigma_percept_7 + stigma_percept_2
GAD =~ nerveux + inquiet + inquiet_exces + difficult_detente + agitation + tendance_contrarie + sentiment_peur
'

fit_GAD_Missing <- cfa(
  cfa_model_GAD,
  data = data_gad,
  estimator = "MLR",
  missing = "fiml"  #management of missing values, as there are many for the GAD-7
)

#summary(fit_GAD_Missing, fit.measures = TRUE, standardized = TRUE, rsquare = TRUE)

fitMeasures(fit_GAD_Missing, c("cfi", "tli", "rmsea", "srmr"))

```


#### with correlation = 1

```{r}
cfa_GAD_corr <- '
BIPQ =~ stigma_percept_1 + stigma_percept_5 + stigma_percept_6 + 
        stigma_percept_8 + stigma_percept_3 + stigma_percept_4 + 
        stigma_percept_7 + stigma_percept_2

GAD =~ nerveux + inquiet + inquiet_exces + difficult_detente + 
       agitation + tendance_contrarie + sentiment_peur

# correlation constrained to 1
BIPQ ~~ 1*GAD
'

fit_GAD_corr <- cfa(
  cfa_GAD_corr,
  data = data_gad,
  estimator = "MLR",
  missing = "fiml" 
)

#summary(fit_GAD_corr, fit.measures = TRUE, standardized = TRUE)
fitMeasures(fit_GAD_corr, c("cfi", "tli", "rmsea", "srmr"))

# test

lavTestLRT(fit_GAD_Missing, fit_GAD_corr)

```

#### 1 factor model 
```{r}
cfa_model_GAD_1 <- '
BIPQGAD =~ stigma_percept_1 + stigma_percept_5 + stigma_percept_6 + stigma_percept_8 + stigma_percept_3 + stigma_percept_4 + stigma_percept_7 + stigma_percept_2
 + nerveux + inquiet + inquiet_exces + difficult_detente + agitation + tendance_contrarie + sentiment_peur
'


fit_GAD_Missing_1 <- cfa(
  cfa_model_GAD_1,
  data = data_gad,
  estimator = "MLR",
  missing = "fiml"
)
#summary(fit_GAD_Missing_1, fit.measures = TRUE, standardized = TRUE, rsquare = TRUE)


fitMeasures(fit_GAD_Missing_1, c("cfi", "tli", "rmsea", "srmr"))
```


## Discriminant validity 

### Simple correlations 
```{r}
cor_spearman <- cor(dat_bis$Score_BIPQ, dat_bis$Score_autostigma, method = "spearman", use = "complete.obs")
cor.test(dat_bis$Score_BIPQ, dat_bis$Score_autostigma, method = "spearman")


```

### Latent model 

#### Simple model (free correlation)
```{r}
var_sss <- c("id_unik", "Score_autostigma", "stigma_auto_1",	"stigma_auto_2",	"stigma_auto_3",	"stigma_auto_4",	"stigma_auto_5",	"stigma_auto_6",	"stigma_auto_7", "stigma_auto_8", "stigma_auto_9",
             "Score_BIPQ", "stigma_percept_1", "stigma_percept_2", "stigma_percept_3", "stigma_percept_4","stigma_percept_5", "stigma_percept_6", "stigma_percept_7", "stigma_percept_8"
)
data_sss <-dat[, c(var_sss)]

cfa_model_SSS <- '
BIPQ =~ stigma_percept_1 + stigma_percept_5 + stigma_percept_6 + stigma_percept_8 + stigma_percept_3 + stigma_percept_4 + stigma_percept_7 + stigma_percept_2
SSS =~ stigma_auto_1 + stigma_auto_2 + stigma_auto_3 + stigma_auto_4 + stigma_auto_5 + stigma_auto_6 + stigma_auto_7 + stigma_auto_8 + stigma_auto_9
'
fit_SSS <- cfa(cfa_model_SSS, data = data_sss, estimator = "MLR")
#summary(fit_SSS, fit.measures = TRUE, standardized = TRUE, rsquare = TRUE)
fitMeasures(fit_SSS, c("cfi", "tli", "rmsea", "srmr"))

```


#### with correlation = 1
```{r}

cfa_SSS_corr <- '
BIPQ =~ stigma_percept_1 + stigma_percept_5 + stigma_percept_6 + 
        stigma_percept_8 + stigma_percept_3 + stigma_percept_4 + 
        stigma_percept_7 + stigma_percept_2

SSS =~ stigma_auto_1 + stigma_auto_2 + stigma_auto_3 + stigma_auto_4 + stigma_auto_5 + stigma_auto_6 + stigma_auto_7 + stigma_auto_8 + stigma_auto_9

# correlation constrained to 1
BIPQ ~~ 1*SSS
'

fit_SSS_corr <- cfa(
  cfa_SSS_corr,
  data = data_sss,
  estimator = "MLR"
)

#summary(fit_SSS_corr, fit.measures = TRUE, standardized = TRUE)
fitMeasures(fit_SSS_corr, c("cfi", "tli", "rmsea", "srmr"))

# test 

lavTestLRT(fit_SSS, fit_SSS_corr)


```


#### 1 factor model

```{r}

cfa_model_SSS_1 <- '
BIPQSSS =~ stigma_percept_1 + stigma_percept_5 + stigma_percept_6 + stigma_percept_8 + stigma_percept_3 + stigma_percept_4 + stigma_percept_7 + stigma_percept_2
 + stigma_auto_1 + stigma_auto_2 + stigma_auto_3 + stigma_auto_4 + stigma_auto_5 + stigma_auto_6 + stigma_auto_7 + stigma_auto_8 + stigma_auto_9
'
fit_SSS_1 <- cfa(cfa_model_SSS_1, data = data_sss, estimator = "MLR")
#summary(fit_SSS_1, fit.measures = TRUE, standardized = TRUE, rsquare = TRUE)

fitMeasures(fit_SSS_1, c("cfi", "tli", "rmsea", "srmr"))
```


# S6. Incremental validity{.tabset} 

## With anova 
```{r}

dat_bis <- dat %>%
  dplyr::select(score_PHQ9, Score_autostigma, Score_BIPQ, Score_whodas, score_GAD7, age, Sexe)

dat_clean <- dat_bis[complete.cases(dat_bis[, c(
  "age",
  "Sexe",
  "Score_whodas",
  "score_PHQ9",
  "Score_autostigma",
  "score_GAD7",
  "Score_BIPQ"
)]), ]

# Model A : WHODAS ~ Age + Sex
modelA <- lm(Score_whodas ~ age + Sexe, data = dat_clean)

# Model B : WHODAS ~ Age + Sex + PHQ9 + SSS + GAD
modelB <- lm(Score_whodas ~ age + Sexe + score_PHQ9 + Score_autostigma + score_GAD7, data = dat_clean)

# Model C : WHODAS ~ Age + Sex + PHQ9 + SSS + GAD + BIPQ
modelC <- lm(Score_whodas ~ age + Sexe + score_PHQ9 + Score_autostigma + score_GAD7 + Score_BIPQ, data = dat_clean)

anova_result <- anova(modelA, modelB, modelC)
anova_result


```

```{r}
r2 <- function(m) summary(m)$r.squared

model1 <- lm(
  Score_whodas ~ age + Sexe,
  data = dat_clean
)

model2 <- lm(
  Score_whodas ~ age + Sexe + score_PHQ9 + Score_autostigma + score_GAD7,
  data = dat_clean
)

model3 <- lm(
  Score_whodas ~ age + Sexe + score_PHQ9 + Score_autostigma + score_GAD7 + Score_BIPQ,
  data = dat_clean
)

r2_table <- data.frame(
  Model = c("Step 1: age + sex",
            "Step 2: + PHQ-9 + SSS-S + GAD-7",
            "Step 3: + BIPQ"),
  R2 = c(r2(model1), r2(model2), r2(model3))
)

r2_table$Delta_R2 <- c(NA, diff(r2_table$R2))

r2_table$R2_percent <- round(r2_table$R2 * 100, 2)
r2_table$Delta_R2_percent <- round(r2_table$Delta_R2 * 100, 2)

r2_table %>% kbl() %>%
  kable_styling()%>%
  column_spec(1, bold = TRUE)
```

```{r, echo=FALSE}

r2_1 <- r2(model1)
r2_2 <- r2(model2)
r2_3 <- r2(model3)
# Ordre des composants
order_blocks <- c(
  "Age + sex",
  "PHQ-9 + SSS-S + GAD-7",
  "BIPQ",
  "Unexplained"
)

fill_blocks <- c(
  "Age + sex"                    = "#3A67A4",
  "PHQ-9 + SSS-S + GAD-7"  = "#B2A5D1",
  "BIPQ"                         = "#29A274",
  "Unexplained"                  = "#E9E7E1"
)

# Table des incréments
block_table <- tibble(
  Component = c(
    "Age + sex",
    "PHQ-9 + SSS-S + GAD-7",
    "BIPQ",
    "Unexplained"
  ),
  Value = c(
    r2_1,
    r2_2 - r2_1,
    r2_3 - r2_2,
    1 - r2_3
  )
) %>%
  mutate(
    Percent = Value * 100,
    Label = sprintf("%.2f%%", Percent),
    Component = factor(Component, levels = order_blocks)
  ) %>%
  arrange(Component) %>%
  mutate(
    xmin = lag(cumsum(Percent), default = 0),
    xmax = cumsum(Percent),
    xmid = (xmin + xmax) / 2,
    
    # Label BIPQ placé au-dessus car segment trop petit
    label_x = ifelse(Component == "BIPQ", xmax + 3, xmid),
    label_y = ifelse(Component == "BIPQ", 1.32, 1)
  )


ggplot(block_table) +
  geom_rect(
    aes(
      xmin = xmin,
      xmax = xmax,
      ymin = 0.8,
      ymax = 1.2,
      fill = Component
    )
  ) +
  geom_text(
    aes(
      x = label_x,
      y = label_y,
      label = Label
    ),
    size = 3.3
  ) +
  geom_segment(
    data = block_table %>% filter(Component == "BIPQ"),
    aes(
      x = xmid,
      xend = label_x,
      y = 1.18,
      yend = 1.27
    ),
    linewidth = 0.3
  ) +
  scale_fill_manual(
    values = fill_blocks,
    breaks = order_blocks[order_blocks != "Unexplained"],
    name = NULL
  ) +
  scale_x_continuous(
    breaks = seq(0, 100, 10),
    labels = function(x) paste0(x, "%"),
    limits = c(0, 105),
    expand = c(0, 0)
  ) +
  labs(
    x = "Percentage of WHODAS-12 variance",
    y = NULL,
    title = sprintf("WHODAS-12 — hierarchical explained variance, total R² = %.2f%%", r2_3 * 100)
  ) +
  theme_minimal(base_size = 11) +
  theme(
    legend.position = "bottom",
    legend.title = element_blank(),
    panel.grid.major.y = element_blank(),
    panel.grid.minor = element_blank(),
    axis.text.y = element_blank(),
    axis.ticks.y = element_blank()
  )
```


```{r}

r2 <- function(m) summary(m)$r.squared

model_full <- lm(
  Score_whodas ~ age + Sexe + score_PHQ9 + Score_autostigma + score_GAD7 + Score_BIPQ,
  data = dat_clean
)

r2_full <- r2(model_full)

unique_table_block <- tibble(
  Predictor = c(
    "Age + sex",
    "PHQ-9",
    "Self-stigma",
    "GAD-7",
    "BIPQ"
  ),
  Unique_R2 = c(
    r2_full - r2(lm(
      Score_whodas ~ score_PHQ9 + Score_autostigma + score_GAD7 + Score_BIPQ,
      data = dat_clean
    )),
    
    r2_full - r2(lm(
      Score_whodas ~ age + Sexe + Score_autostigma + score_GAD7 + Score_BIPQ,
      data = dat_clean
    )),
    
    r2_full - r2(lm(
      Score_whodas ~ age + Sexe + score_PHQ9 + score_GAD7 + Score_BIPQ,
      data = dat_clean
    )),
    
    r2_full - r2(lm(
      Score_whodas ~ age + Sexe + score_PHQ9 + Score_autostigma + Score_BIPQ,
      data = dat_clean
    )),
    
    r2_full - r2(lm(
      Score_whodas ~ age + Sexe + score_PHQ9 + Score_autostigma + score_GAD7,
      data = dat_clean
    ))
  )
) %>%
  mutate(
    Percent = Unique_R2 * 100,
    Label = paste0(round(Percent, 2), "%")
  )

unique_table_block %>% kbl() %>%
  kable_styling()%>%
  column_spec(1, bold = TRUE)
```

```{r, echo=FALSE}
# =========================
# Table des variances uniques
# =========================
# J'utilise ici tes valeurs déjà calculées
unique_plot <- tibble(
  Component = c("Age + sex", "PHQ-9", "Self-stigma", "GAD-7", "BIPQ"),
  Percent = c(1.35123818, 6.55370848, 0.20984138, 2.31791288, 0.09963607)
)

# Ordre d'affichage
order_unique <- c("Age + sex", "PHQ-9", "Self-stigma", "GAD-7", "BIPQ")


# Préparation des positions
unique_plot <- unique_table_block %>%
  transmute(
    Component = Predictor,
    Percent = Percent
  ) %>%
  mutate(
    Component = recode(Component, "Self-stigma" = "SSS-S")
  ) %>%
  arrange(desc(Percent)) %>%
  mutate(
    Component = factor(Component, levels = Component),
    Label = sprintf("%.2f%%", Percent),
    xmin = lag(cumsum(Percent), default = 0),
    xmax = cumsum(Percent),
    xmid = (xmin + xmax) / 2,

    label_text = case_when(
      Component == "SSS-S" ~ paste0("SSS-S: ", Label),
      Component == "BIPQ"  ~ paste0("BIPQ: ", Label),
      TRUE ~ Label
    ),

    label_x = case_when(
      Component == "SSS-S" ~ xmid,       # SSS-S au-dessus
      Component == "BIPQ"  ~ xmax + 0.8, # BIPQ à droite
      TRUE ~ xmid
    ),

    label_y = case_when(
      Component == "SSS-S" ~ 1.55,
      Component == "BIPQ"  ~ 1.00,
      TRUE ~ 1.00
    ),

    hjust_label = case_when(
      Component == "BIPQ" ~ 0,
      TRUE ~ 0.5
    )
  )

fill_unique <- c(
  "PHQ-9"     = "#3A67A4",
  "GAD-7"     = "#60CDA0",
  "Age + sex" = "#B2A5D1",
  "SSS-S"     = "#E68613",
  "BIPQ"      = "#7B2CBF"
)

ggplot(unique_plot) +
  geom_rect(
    aes(
      xmin = xmin,
      xmax = xmax,
      ymin = 0.8,
      ymax = 1.2,
      fill = Component
    )
  ) +

  # Labels
  geom_text(
    aes(
      x = label_x,
      y = label_y,
      label = label_text,
      hjust = hjust_label
    ),
    size = 3.3
  ) +

  # Trait vertical pour SSS-S
  geom_segment(
    data = unique_plot %>% filter(Component == "SSS-S"),
    aes(
      x = xmid,
      xend = xmid,
      y = 1.2,
      yend = 1.45
    ),
    linewidth = 0.3
  ) +

  # Trait horizontal pour BIPQ
  geom_segment(
    data = unique_plot %>% filter(Component == "BIPQ"),
    aes(
      x = xmax,
      xend = label_x - 0.15,
      y = 1.0,
      yend = 1.0
    ),
    linewidth = 0.3
  ) +

  scale_fill_manual(
    values = fill_unique,
    breaks = levels(unique_plot$Component),
    name = NULL,
    guide = guide_legend(nrow = 1, byrow = TRUE)
  ) +

  scale_x_continuous(
    breaks = seq(0, 14, 1),
    labels = function(x) paste0(x, "%"),
    limits = c(0, 14),
    expand = c(0, 0)
  ) +

  coord_cartesian(ylim = c(0.75, 1.70), clip = "off") +

  labs(
    x = "Unique variance explained in WHODAS-12",
    y = NULL,
    title = "WHODAS-12 — unique contributions",
    subtitle = ""
  ) +

  theme_minimal(base_size = 11) +
  theme(
    legend.position = "bottom",
    legend.title = element_blank(),
    legend.spacing.x = unit(0.8, "cm"),
    legend.key.width = unit(0.9, "cm"),
    legend.text = element_text(size = 9),
    panel.grid.major.y = element_blank(),
    panel.grid.minor = element_blank(),
    axis.text.y = element_blank(),
    axis.ticks.y = element_blank()
  )
```




## with yhat
```{r}
## Commonality analysis 

res_lm <- lm(Score_whodas ~ age + Sexe + score_PHQ9 + Score_autostigma + score_GAD7 + Score_BIPQ,
             data = dat_clean)
res_dom <- calc.yhat(res_lm)
boot.out = boot(data=dat_clean, yhat::boot.yhat, 1000, 
                lmOut=res_lm, regrout0=res_dom)

boot_results = booteval.yhat(res_dom, boot.out,bty="perc",.95,3)

results_filtered <- boot_results$combCIaps["age,Sexe,score_PHQ9,Score_autostigma,score_GAD7", "Score_BIPQ.Inc"] # First, the proportion of Whodas variance explained by the three questions, then the incremental validity of the BIPQ.

print(results_filtered)

```





